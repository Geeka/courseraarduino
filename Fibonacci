#include <studio.h> ;

int main()
{
   
    int num =0;
    int nextnum =1;
    printf ("%d ",num);
    printf ("%d ",nextnum); 
    int i=0;
    while(i<4) {
    int result = num + nextnum;
    printf ("%d ", result);
        num=nextnum;
        nextnum=result;
        i++;
    }
return 0;
}

